# Projects excluded by default from run-maven / run-jqa.
# One project path per line (relative to ${MAVEN_PROJECTS_DIR}, default `maven`).
# Lines starting with `#` and blank lines are ignored.
#
# Override at runtime by setting EXCLUDE_PROJECTS (space-separated) or
# EXCLUDE_PROJECTS="" to disable exclusion entirely.

# Sandbox / demo projects that don't build standalone.
studies/consumer-pom
studies/maven-eventsound-extension
studies/maven-extension-demo

# Legacy SVN-imported tool with stale 1.0-SNAPSHOT dependency.
svn/repository-tools

# Retired upstream: pom name is "(RETIRED) Doxia Tools :: Book Maven Plugin",
# final-release/retirement commit 2026-05-25. Not worth chasing to M4-green.
# (Only this one plugin is retired; the rest of doxia/ stays in the survey.)
doxia/tools/doxia-book-maven-plugin
