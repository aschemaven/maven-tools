# Projects whose builds are known to be intermittently flaky (e.g. due to
# environment-sensitive integration tests, race conditions, or network
# timing). Listed projects get FLAKY_RETRY_DEFAULT (default 2) automatic
# retries when their build fails, even without --retry on the CLI.
#
# One project path per line (relative to ${MAVEN_PROJECTS_DIR}, default `maven`).
# Lines starting with `#` and blank lines are ignored.
#
# Override at runtime by setting FLAKY_PROJECTS (space-separated) or
# FLAKY_PROJECTS="" to disable per-project flaky handling.
#
# Document the actual cause of flakiness in ~/wrk/maven/maven-bugfixing/queue/
# before adding a project here -- the retry list is not a place to hide
# permanently broken projects.

# ApacheTransporterTest.testGet_ProxyAuthenticated occasionally fails locally
# with "NoHttpResponse bad.localhost:1 failed to respond" instead of the
# expected HTTP-proxy auth error. Environment-sensitive DNS/HTTP behavior.
# See ~/wrk/maven/maven-bugfixing/queue/core-resolver-apache-transport-lokal-2026-06-07.md
core/resolver

# wagon-providers HTTP/WebDAV redirect tests intermittently fail with
# "TransferFailed transfer failed for http://localhost:<port>/resource";
# different test fails each run (Lightweight, WebDav, ...), classic
# loopback-timing flakiness on macOS.
# See ~/wrk/maven/maven-bugfixing/queue/misc-wagon-http-tests-lokal-2026-06-07.adoc
misc/wagon
